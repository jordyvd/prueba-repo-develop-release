name: Check branch sync

on:
  pull_request:
    branches:
      - release

jobs:
  check-sync:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Fetch branches
        run: |
          git fetch origin release
          git fetch origin ${{ github.head_ref }}

      - name: Check if release is ancestor of PR branch
        run: |
          git merge-base --is-ancestor origin/release origin/${{ github.head_ref }}
